# GitHub Release

**Title:** v2.5.0
**Tag:** v2.5.0

---

## Description (copy below)

<!-- gh-release-start -->
# What's Changed

This release adds incremental queueable chain composition, fixes a set of chain-result and inner-class deep-clone bugs, and is the first time Async Lib is distributed as a `btcdev`-namespaced unlocked package.

## 🚀 New Features

- **`Async.queueable()` (no-arg) builder.** Compose a queueable chain when the set of jobs depends on runtime conditions. Calling `enqueue()` on a builder with zero jobs is a safe no-op. (#28)

  ```apex
  QueueableBuilder builder = Async.queueable();
  if (needsJob1) { builder.chain(new Job1()); }
  if (needsJob2) { builder.chain(new Job2()); }
  if (needsJob3) { builder.chain(new Job3()); }
  builder.enqueue();
  ```

- **First packaged release.** Async Lib is now distributed as a `btcdev`-namespaced unlocked package. See [Installation](https://async.beyondthecloud.dev/introduction/installation).

## ✨ Improvements

- `cloneForDeepCopy` override signature refined — see [Deep Clone in Packages](https://async.beyondthecloud.dev/explanations/deep-clone-in-packages).

## 🐛 Bug Fixes

- `Result.salesforceJobId` now reports the actually-enqueued first-chain job id (previously the last builder-supplied job, which was never enqueued).
- `SchedulableManager.schedule()` + `skipWhenAlreadyScheduled()` returns an empty `List<Async.Result>` on duplicate (previously `null`).
- `QueueableJob.cloneForDeepCopy()` resolves inner job classes correctly (previously NPE'd because outer-class names were mistaken for namespaces).
- Documented `cloneForDeepCopy` override pattern for namespaced consumers, fixing the JSONException seen when calling `.deepClone()` from a package install.

## 📚 Documentation

- **NEW** [Deep Clone in Packages](https://async.beyondthecloud.dev/explanations/deep-clone-in-packages) — when and how to override `cloneForDeepCopy` for namespaced consumers.
- Installation page covers the unlocked-package path and the `btcdev` namespace prefix callout.
- `Async.Result` table documents the `Result.job` field and the corrected `salesforceJobId` semantics.
- [Documentation](https://async.beyondthecloud.dev) · [Installation](https://async.beyondthecloud.dev/introduction/installation.html)

## ⚠️ Upgrade notes from v2.4.0

- `Result.salesforceJobId` is no longer `null` for chained-then-enqueued flows. If your code branched on `result.salesforceJobId == null` to detect that, switch to inspecting `result.queueableChainState.jobs`.
- `SchedulableBuilder.schedule()` with `skipWhenAlreadyScheduled()` returns an empty list, not `null`. Replace `result == null` guards with `result.isEmpty()`.
- If you previously source-deployed the library and switch to the package install, replace `Async.*` / `QueueableJob` / etc. with `btcdev.Async.*` / `btcdev.QueueableJob`.

## Install

```
https://login.salesforce.com/packaging/installPackage.apexp?p0=04tP60000038pYHIAY
```
<!-- gh-release-end -->

---

# LinkedIn Post (copy below)

New Async Lib release v2.5.0 is here! 🚀

Async.queueable() no-arg builder — compose a queueable chain incrementally when the set of jobs depends on runtime conditions. No more sentinel first job, no more workarounds for the "maybe 0/1/2 jobs" case.

🚀 Async.queueable() — zero-arg factory + conditional .chain(job); empty enqueue is a safe no-op
📦 First packaged release — Async Lib is now an unlocked package with the btcdev namespace
🐛 Result.salesforceJobId fix — reports the actually-enqueued first-chain job id
🐛 SchedulableManager.schedule() + skipWhenAlreadyScheduled() returns empty list (was null) on duplicate
🐛 QueueableJob.cloneForDeepCopy() now resolves inner job classes correctly

----
Release: https://github.com/beyond-the-cloud-dev/async-lib/releases/tag/v2.5.0
Github: https://github.com/beyond-the-cloud-dev/async-lib
Documentation: https://async.beyondthecloud.dev

---

# Reference: Commits since v2.4.0

9908a89 Document Result.job and update salesforceJobId description
ea2a93f Add Async.queueable() no-arg builder for incremental chaining
2ff435d Add local agent tooling paths to gitignore
998f37a Fix Result.salesforceJobId, schedule null return, and inner-class deep clone
b830935 Bump the npm_and_yarn group across 1 directory with 5 updates
6f779c6 Bump the npm_and_yarn group across 1 directory with 2 updates
c48bdb2 Add new updates for unlocked package.
5a218f4 Paackage update.
38fe7c5 Update deep clone override definition.
41b18de Package update.
67a37d7 Workaround JSON exception from packages.
2b5e76d New package version.
ec1b93c Add namespace for unlocked package.
9598980 Bump fast-xml-parser in the npm_and_yarn group across 1 directory
4c22c6f package version created.
8d2c5c5 Script updates.
261befa Fix project json def.
abd8356 Script to create unlocked package.
e03f0bc API version update and package created.
f595a45 Bump lodash in the npm_and_yarn group across 1 directory

# Reference: Changed files

 .gitignore                                         |   6 +-
 force-app/main/default/classes/Async.cls           |   4 +
 force-app/main/default/classes/AsyncTest.cls       | 167 ++++++++-
 .../batch/BatchableSchedulable.cls-meta.xml        |   2 +-
 .../default/classes/queue/QueueableBuilder.cls     |  13 +-
 .../main/default/classes/queue/QueueableJob.cls    |  29 +-
 .../default/classes/queue/QueueableManager.cls     |  16 +-
 .../classes/schedule/CronBuilder.cls-meta.xml      |   2 +-
 .../classes/schedule/SchedulableManager.cls        |   7 +-
 package-lock.json                                  | 376 +++++++++++++--------
 package.json                                       |   2 +-
 scripts/create-unlocked-package-version.sh         |  57 ++++
 sfdx-project.json                                  |  16 +-
 website/.vitepress/config.mts                      |   4 +
 website/api/queueable.md                           | 110 ++++--
 website/explanations/deep-clone-in-packages.md     | 113 +++++++
 website/explanations/job-cloning.md                |   4 +
 website/introduction/installation.md               |  16 +
 18 files changed, 742 insertions(+), 202 deletions(-)
